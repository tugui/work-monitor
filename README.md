# work-monitor

Execute the install command from the project root directory, then double-click dist/WorkMonitor.exe to launch the application. Alerts you to take a rest after extended periods of work.

```bash
# intall
./build_windows.bat
```
